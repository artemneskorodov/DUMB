function _test(param)
{
    variable ind = param;
    variable result = 0;
    while ( ind < 1000 )
    {
        variable inv = param * 10 + param * param * param;
        result = result * inv + inv * 4;
        ind = ind + 1;
    }
    return result;
}

function main()
{
    variable result = _test(10);
    output(result, "Result = ");
    return 0;
}
