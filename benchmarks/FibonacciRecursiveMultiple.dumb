function _test(num)
{
    if ( num == 0 )
    {
        return 0;
    }
    if ( num == 1 )
    {
        return 1;
    }
    return _test(num - 1) + _test(num - 2);
}

function main()
{
    variable result = _test(10);
    output(result, "Result = ");
}
