function _test(parameter)
{
    variable a = parameter + 10;
    variable b = a + 5;
    variable c = b * 2;
    variable d = c + 100;
    variable e = d * 2;

    return c;
}

function main()
{
    variable result = _test(10);
    output(result, "Result = ");
}
